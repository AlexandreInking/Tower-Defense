using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Michael : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hola Perro");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
